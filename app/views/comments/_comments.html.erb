<h3>Comments</h3>
<br>
<% @post.comments.each do |comment| %>
  <div class='well'>
    <p><strong><%= comment.username %></strong>: <%= comment.body %> 
    <%= link_to '[X]', [comment.post, comment], method: :delete, data: {confirm: 'You sure?'} %> </p>
  </div>
<% end %>